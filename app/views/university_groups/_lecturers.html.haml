%table.table.table-bordered
  - lecturers.in_groups_of(5, false) do |lecturers_row|
    %tr
      - lecturers_row.each do |lecturer|
        %td
          = check_box_tag("university_group[lecturer_ids][]", lecturer.id,
                 university_group.lecturer_ids.include?(lecturer.id))
          = lecturer.fullname
